<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xzit.spring.mapper.SuperAdminMapper">
    <select id="findUserBy" parameterType="String" resultType="com.xzit.spring.entity.SuperAdmin">
        select * from superadmin where id=#{account} and password=#{password}
    </select>
</mapper>